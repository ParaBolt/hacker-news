import React from 'react'
import Typography from '@material-ui/core/Typography'

export const Loader: React.FC = () => (
	<Typography variant="h5" component="p" key={0}>
		Loading...
	</Typography>
)
